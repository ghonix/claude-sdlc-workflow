---
description: Create a project proposal with a Product Manager perspective — problem definition, goals, success metrics, and strategic fit
argument-hint: "Idea or feature description (e.g. 'add dark mode', 'reduce signup friction', 'new onboarding flow')"
---

# PM Proposal

Turn a rough idea into a structured project proposal.

Idea: $ARGUMENTS

Launch the `adlc-pm` agent:

```
Agent(subagent_type="adlc-pm", description="PM Proposal: $ARGUMENTS", prompt="$ARGUMENTS")
```
